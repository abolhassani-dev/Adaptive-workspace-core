# Never commit secrets or local machine state
.env
.env.*
*.pem
*.key
credentials*.json
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db

# Node dependencies (installed by Cloudflare at build time)
node_modules/
.wrangler/
